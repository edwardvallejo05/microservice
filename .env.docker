# Variables de entorno para Docker Compose
# Estas variables serán sobrescritas por las definidas en docker-compose.yml

# Configuración de MariaDB para contenedores
DB_HOST=mariadb
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=root
DB_NAME=usuarios_db

# Puertos de los servicios
USER_SERVICE_PORT=5001
ORDER_SERVICE_PORT=5002

# URL del servicio de usuarios para comunicación interna
USER_SERVICE_URL=http://user-service:5001

# Entorno de ejecución
NODE_ENV=production